export const playlistSelector = (state) => state.audioplayer.playlist

export const shufflePlaylistSelector = (state) => state.audioplayer.shufflePlaylist

export const trackSelector = (state) => state.audioplayer.track

export const isTrackPlaySelector = (state) => state.audioplayer.playing
